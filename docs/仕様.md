# ブログ 仕様書

## プロジェクト概要

- **サイト名**: u83 said so.
- **URL**: https://u83ism.github.io/blog/
- **リポジトリ**: https://github.com/u83ism/blog
- **運営開始**: 2000年（移行元コンテンツ）
- **ホスティング**: GitHub Pages
- **管理人**: ゆうやみ（@u83unlimited）

DLB（Don't Look Back）から切り出した雑記部分を独立サイトとして再構築したもの。
2000〜2014年のアーカイブと、以降の新規投稿を一元管理する。

## エコシステムにおける位置づけ

```
ゆうやみポータル (https://u83-portal.web.app/)
├── Don't Look Back (DLB)
├── 雑記ブログ (このリポジトリ) ← 新規構築
├── Cradle 2 Grave
├── OBRN
└── 21世紀パワーボム
```

---

## 技術スタック

| 項目 | 採用技術 |
|---|---|
| フレームワーク | Astro 6 |
| 言語 | TypeScript |
| コンテンツ管理 | Astro Content Collections（glob loader） |
| スタイリング | CSS（Scoped / Global） |
| デプロイ | GitHub Actions → GitHub Pages |
| パッケージマネージャ | npm |

### Astro設定方針

- `output: 'static'`（SSG）
- `base: '/blog'`（GitHub Pagesのリポジトリ名）
- `trailingSlash: 'always'`（DLBと統一。`import.meta.env.BASE_URL` が `/blog/` になる）
- `prefetch: true`
- `site: 'https://u83ism.github.io'`

### View Transitions

`Layout.astro` で `<ClientRouter />` を使用し、ページ遷移時にAstroのView Transitionsを有効化している。

---

## ディレクトリ構成

```
blog/
├── src/
│   ├── content/
│   │   └── posts/
│   │       ├── 2000/
│   │       │   ├── 2000-04-08.md
│   │       │   └── ...
│   │       ├── 2001/
│   │       └── ...（年ごとのサブディレクトリ）
│   ├── content.config.ts           # Collectionスキーマ定義（Astro 6: srcルート直下）
│   ├── pages/
│   │   ├── index.astro             # 記事一覧 1ページ目
│   │   ├── posts/
│   │   │   └── [...slug].astro     # 個別記事ページ
│   │   ├── page/
│   │   │   └── [page].astro        # 記事一覧 2ページ目以降
│   │   ├── tags/
│   │   │   ├── index.astro         # タグ一覧
│   │   │   └── [tag].astro         # タグ別記事一覧
│   │   ├── archive/
│   │   │   ├── index.astro         # アーカイブトップ（年一覧）
│   │   │   ├── [year].astro        # 年別アーカイブ
│   │   │   └── [year]/
│   │   │       └── [month].astro   # 月別アーカイブ
│   │   ├── sitemap.astro           # HTMLサイトマップ（XMLサイトマップの代替）
│   │   └── rss.xml.ts              # RSSフィードエンドポイント
│   ├── layouts/
│   │   ├── Layout.astro            # 共通レイアウト（Header・Nav・Footer・OGP・View Transitions）
│   │   └── PostLayout.astro        # 記事ページレイアウト（パンくず・読了時間・関連記事・前後ナビ）
│   ├── components/
│   │   ├── Header.astro            # サイトタイトル・キャプション
│   │   ├── Nav.astro               # グローバルナビゲーション（モバイル対応トグル付き）
│   │   ├── Footer.astro            # フッター
│   │   ├── PostList.astro          # 記事一覧の表示
│   │   ├── TagList.astro           # タグ一覧の表示
│   │   ├── ArchiveNav.astro        # 年別アーカイブナビゲーション
│   │   └── Breadcrumb.astro        # パンくずリスト（JSON-LD付き）
│   ├── lib/
│   │   ├── posts.ts                # 記事操作ユーティリティ（純粋関数）
│   │   └── tags.ts                 # 正規タグ定義
│   └── styles/
│       └── global.css              # グローバルCSSスタイル
├── public/
├── docs/
├── astro.config.ts
├── tsconfig.json
└── package.json
```

---

## Content Collections スキーマ

`src/content.config.ts`（Astro 6形式）：

```typescript
import { defineCollection, z } from 'astro:content';
import { glob } from 'astro/loaders';

const posts = defineCollection({
  loader: glob({ pattern: '**/*.md', base: './src/content/posts' }),
  schema: z.object({
    date: z.date(),
    tags: z.array(z.string()).default([]),
    title: z.string().optional(), // 省略時はdateから自動生成
  }),
});

export const collections = { posts };
```

### frontmatterサンプル

タイトルなし（日付がタイトルになる）：

```yaml
---
date: 2000-04-08
tags: ["ゲーム", "モンコレ"]
---

初めて雑記を書いてみたんですけど……
```

タイトルあり：

```yaml
---
date: 2000-04-08
title: モンスター・コレクション大会に出てきた
tags: ["ゲーム", "モンコレ"]
---

初めて雑記を書いてみたんですけど……
```

- `title` は省略可能。省略時は `YYYY年MM月DD日` の形式で自動生成する
- `tags` は省略時に空配列

---

## ロジック層（`src/lib/`）

### `src/lib/rehype-image-link.ts`

Markdown内の `<img>` 要素を `<a target="_blank">` で包むrehypeプラグイン。
ビルド時に `astro.config.ts` の `rehypePlugins` として登録されている。

- `![alt](url)` と書くだけで、クリック時に新しいタブで画像が開く
- すでに `[![alt](url)](link)` のように手動でリンクを書いている場合はスキップ
- このプラグインの後に `rehypeCloudinaryResize` が動き、`<img src>` だけ変換URLに差し替えられる

### `src/lib/rehype-cloudinary-resize.ts`

Cloudinary画像の `<img src>` に変換パラメータを挿入するrehypeプラグイン。
`rehypeImageLink` の後に適用することで `<a href>` はオリジナルURL・`<img src>` はリサイズURLになる。

- 対象: `https://res.cloudinary.com/.../image/upload/v{version}/...` 形式のURL
- パラメータ: `h_360,c_limit,q_auto,f_auto`

### `src/lib/posts.ts`

記事操作に関する純粋関数をまとめたモジュール。

| 関数名 | 役割 |
|--------|------|
| `formatDateTitle(date)` | `Date` → `YYYY年MM月DD日` 形式の文字列 |
| `formatDateDisplay(date)` | `Date` → `YYYY/MM/DD` 形式の文字列 |
| `resolveTitle(post)` | 記事タイトルを返す（`title` 省略時は日付から生成） |
| `buildPostUrl(base, id)` | 記事 id から個別ページURLを生成（`.md` 拡張子を除去） |
| `sortPostsByDateDesc(posts)` | 記事を日付降順でソート |
| `collectTags(posts)` | 全タグを集計し出現数降順で返す |
| `groupPostsByYear(posts)` | 記事を年ごとにグループ化（降順） |
| `groupPostsByMonth(posts)` | 記事を月ごとにグループ化（降順） |
| `findRelatedPosts(current, posts, limit?)` | 共通タグ数スコアで類似記事を返す（デフォルト上位5件） |
| `estimateReadingTime(body)` | Markdown本文の文字数から読了時間（分）を推定（500字/分） |

---

## ページ仕様

### 記事一覧（`/`）

- 全記事を日付降順で表示
- ページネーション（1ページあたり20件）
  - 1ページ目は `index.astro` が担当
  - 2ページ目以降は `page/[page].astro` が担当（`/page/2/`、`/page/3/` …）
- サイドバーにタグ一覧・年別アーカイブへのリンク

### 個別記事（`/posts/YYYY/YYYY-MM-DD/`）

- スラッグはファイルパスから自動生成（glob loaderのidから`.md`拡張子を除去）
- 前後記事へのナビゲーション（PostLayout.astro に実装）
- タグの表示
- OGタイプは `article`

### タグ一覧（`/tags/`）

- 全タグを出現数降順で表示
- 各タグに記事件数を表示

### タグ別一覧（`/tags/[tag]/`）

- タグを持つ記事を日付降順で表示
- ヘッダーにタグ名と件数を表示

### アーカイブトップ（`/archive/`）

- 記事が存在する年の一覧を降順で表示
- 各年に記事件数を表示

### 年別アーカイブ（`/archive/[year]/`）

- 指定年の記事を月別にグルーピングして表示
- 月の見出しは `/archive/[year]/[month]/` へのリンク

### 月別アーカイブ（`/archive/[year]/[month]/`）

- 指定年月の記事を日付降順で表示
- サイドバーに年別アーカイブナビを表示

### RSSフィード（`/rss.xml`）

- 全記事を日付降順で配信（`@astrojs/rss`）

### HTMLサイトマップ（`/sitemap/`）

- 全ページへのリンクを列挙したHTMLページ
- 主要ページ・全タグ・全記事（年別グループ）を掲載
- フッターからリンクし、Googleがトップページ起点で辿れるようにしている

---

## レイアウト・UI仕様

### Layout.astro

全ページ共通のシェル。以下を含む：

- `<head>`: OGP・Twitter Card・canonical URL・デフォルト description
- `<Header>`: サイトタイトル（`transition:persist` で遷移時も維持）
- `<Nav>`: グローバルナビゲーション（currentPath を渡してアクティブ表示）
- サイドバーグリッド: `sidebar` スロットがある場合のみ2カラム（PC: 220px + 1fr）
- `<ClientRouter />`: View Transitions

#### サイドバーのレスポンシブ対応

- 900px以下: グリッド解除、サイドバーを上部帯として表示
- サイドバーなしのページ（個別記事など）では1カラム表示

### Nav.astro

- PC: 横並びのタブ型ナビゲーション
- モバイル（600px以下）: 「メニュー」ボタンで開閉するアコーディオン型
- ナビリンク: HOME / タグ一覧 / アーカイブ / 🔗 ゆうやみポータル（外部リンク）

### PostLayout.astro

- サイドバーなし・中央集中型（max-width: 800px）
- パンくずリスト（`Breadcrumb.astro`）
- ヘッダーに投稿日と読了時間（約X分）を表示
- 関連記事（共通タグ数スコア上位5件、タグなし記事では非表示）
- 前後記事ナビゲーション: 日付が新しい方が「次の記事（→）」、古い方が「前の記事（←）」

---

## OGP / SNS メタタグ

`Layout.astro` で全ページに出力。

| プロパティ | 内容 |
|---|---|
| `og:type` | `website`（デフォルト）/ `article`（記事ページ） |
| `og:title` | `ページタイトル \| u83 said so.` |
| `og:description` | Props の `description`（デフォルト: サイト説明文） |
| `og:url` | canonical URL |
| `og:site_name` | `u83 said so.` |
| `og:locale` | `ja_JP` |
| `og:image` | Props の `ogImage`（省略時は出力なし） |
| `twitter:card` | 画像あり: `summary_large_image` / なし: `summary` |

---

## タグ設計方針

移行時に主要タグを事前設計したうえで付与する。自動付けは行わない。
候補トピック（雑記内容から）：ゲーム、プロレス、DDR・音ゲー、アニメ・マンガ、日常、サイト運営、Web技術

---

## サイトマップ

`@astrojs/sitemap` を `astro.config.ts` の integrations に登録済み。ビルド時に `sitemap-index.xml` を自動生成する。

### GitHub Pages における XML サイトマップの制約

**GitHub Pages は XML ファイルを `Content-Type: text/html` で配信するバグがある。**
Google Search Console は Content-Type を厳格にチェックするため、`sitemap-index.xml` を正しく読み込めず「サイトマップを読み込めませんでした」エラーが発生し続ける（Bing は問題なく取得できる）。

この問題は GitHub Pages 側の既知の未解決バグであり、`.nojekyll` や `robots.txt` の設定では解消しない。

**対策として `/sitemap/` に HTML サイトマップを設置している（`src/pages/sitemap.astro`）。**
Google はこのページのリンクを通常のクロールで辿ることができる。

参考記事: [GitHub Pagesのサイトマップがサーチコンソールで取得できない問題について](https://zenn.dev/uv/articles/github-pages-sitemap-content-type-bug)

---

## 画像管理

### ホスティング

画像は **Cloudinary**（外部CDN）にホストする。Gitリポジトリには含めない。

- Markdownでは `![alt](https://res.cloudinary.com/...)` と書くだけでよい
- ビルド時に画像ファイルは不要（URLがそのままHTMLに出力される）
- `public/images/` はローカル作業用の一時置き場として使い、`.gitignore` で除外済み

### アップロードフロー

```
public/images/ に画像を置く
    ↓
npm run upload
    ↓
Cloudinary URL が出力される
    ↓
Markdown に貼り付けて git push
```

### アップロードスクリプト（`scripts/upload-images.ts`）

`npm run upload` で `public/images/` 内の画像を一括アップロードしてURLを出力する。

#### 認証情報

Cloudinaryの認証情報はWindowsの**ユーザー環境変数**に登録する（ファイルに書かない）。

| 変数名 | 内容 |
|--------|------|
| `CLOUDINARY_CLOUD_NAME` | Cloud name |
| `CLOUDINARY_API_KEY` | API Key |
| `CLOUDINARY_API_SECRET` | API Secret |

登録コマンド:
```powershell
[Environment]::SetEnvironmentVariable("CLOUDINARY_CLOUD_NAME", "値", "User")
[Environment]::SetEnvironmentVariable("CLOUDINARY_API_KEY", "値", "User")
[Environment]::SetEnvironmentVariable("CLOUDINARY_API_SECRET", "値", "User")
```

#### 表示サイズの縮小とクリックで拡大表示

`astro.config.ts` の `rehypePlugins` に以下を順番に登録している。

1. **`rehypeImageLink`**（`src/lib/rehype-image-link.ts`）  
   Markdown内の `<img>` を `<a href="オリジナルURL" target="_blank">` で包む。クリックすると新しいタブでオリジナル画像が開く。

2. **`rehypeCloudinaryResize`**（`src/lib/rehype-cloudinary-resize.ts`）  
   Cloudinary URL を持つ `<img>` の `src` に変換パラメータを挿入する。`<a>` のリンク先はオリジナルURLのまま保たれる。  
   適用パラメータ: `h_360,c_limit,q_auto,f_auto`（高さ360px上限・縮小のみ・品質とフォーマット自動）

---

## 将来検討

- **OGP画像の自動生成**: 記事タイトル・日付を `satori`（HTML/CSSをコードでPNGに変換するライブラリ、AIは不使用）でビルド時に静的生成する方式。GitHub Pages環境への対応が必要。

---

## デプロイ

DLBと同じGitHub Actionsワークフローを使用。
`main`ブランチへのpushで自動ビルド・デプロイ。

```yaml
# .github/workflows/deploy.yml（DLBから流用）
on:
  push:
    branches: [main]
jobs:
  deploy:
    # astro build → peaceiris/actions-gh-pages
```
